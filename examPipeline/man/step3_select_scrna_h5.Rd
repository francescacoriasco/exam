% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step3_select_scrna_h5.R
\name{step3_select_scrna_h5}
\alias{step3_select_scrna_h5}
\title{Step 3 — Select scRNA-seq 10x HDF5 files by content (Gene Expression/RNA)}
\usage{
step3_select_scrna_h5(
  data_dir,
  recursive = TRUE,
  pattern = "\\\\.h5$",
  bpparam = BiocParallel::SerialParam(),
  rna_types = c("Gene Expression", "RNA"),
  assume_rna_if_missing = TRUE,
  sort_files = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{data_dir}{Directory to scan for \code{.h5} files.}

\item{recursive}{If TRUE, scan recursively.}

\item{pattern}{Regex for \code{list.files()} (default \code{"\\\\.h5$"}).}

\item{bpparam}{\code{BiocParallelParam} for \code{DropletUtils::read10xCounts()}.}

\item{rna_types}{Feature types treated as scRNA (default \code{c("Gene Expression","RNA")}).}

\item{assume_rna_if_missing}{If TRUE and type metadata missing, assume RNA-only.}

\item{sort_files}{If TRUE, return deterministically sorted paths.}

\item{verbose}{If TRUE, prints progress.}
}
\value{
Character vector of kept \code{.h5} paths.
}
\description{
Scans a directory for 10x Genomics \code{.h5} files and keeps only those that contain
\dQuote{Gene Expression}/\dQuote{RNA} features (via \code{rowData()} columns \code{Type}
or \code{feature_type}). The helper used to inspect file content is defined \emph{inside}
this function (not exported, not separate).
}
