% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/step13_atac_umap_by_pdx.R
\name{step13_atac_umap_by_pdx}
\alias{step13_atac_umap_by_pdx}
\title{Step 13 — ATAC clustering + UMAP, plot colored by PDX}
\usage{
step13_atac_umap_by_pdx(
  atac,
  out_dir,
  pdx_col = NULL,
  pdx_candidates = c("pdx", "PDX", "pdx_scRNA", "pdx_of_origin"),
  resolution = 0.5,
  k_param = NULL,
  max_lsi = 30L,
  dims_use = NULL,
  save_plots = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{atac}{Either a Seurat object or a path to \code{.rds}.}

\item{out_dir}{Output directory for plots.}

\item{pdx_col}{Metadata column name to use for PDX (if NULL, tries common candidates).}

\item{pdx_candidates}{Candidate metadata columns if \code{pdx_col} is NULL.}

\item{resolution}{Clustering resolution.}

\item{k_param}{Neighbor k (NULL = auto).}

\item{max_lsi}{Max LSI components to consider.}

\item{dims_use}{LSI dims to use (NULL = auto, default skips dim 1).}

\item{save_plots}{If TRUE, saves png+pdf.}

\item{verbose}{If TRUE, prints summary.}
}
\value{
Invisible list with \code{atac}, \code{plot}, \code{plot_files}.
}
\description{
Loads the combined ATAC object (Step 12) or accepts it directly, runs TF-IDF/LSI,
clustering and UMAP, then saves a UMAP plot colored by PDX.
}
